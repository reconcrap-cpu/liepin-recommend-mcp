# P00

## Goal
Establish the cross-phase documentation protocol so a fresh instance can recover context without hidden knowledge.

## Actual Changes
- Added roadmap, phase index, phase template, and research markdown placeholders.
- Fixed the startup and completion protocol in markdown rather than in chat-only context.

## Live Validation Evidence
- Verified the repo was empty before scaffolding, so the protocol is now the source of truth for future phases.

## Tests
- Manual validation: reading `roadmap.md -> phase-index.md -> latest passed phase` is sufficient to reconstruct current state.

## Learnings
- The project needs tracked markdown from day one because this workflow intentionally spans multiple fresh instances.

## Pitfalls
- If phase status is not updated immediately after a phase passes, the next instance can start from the wrong point.

## Next Phase Notes
- `P01` must keep public names Liepin-prefixed and reserve the final CLI/MCP command surface even before all behaviors are implemented.
